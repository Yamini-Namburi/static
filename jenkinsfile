pipeline {
     agent any
     stages {
        stage('Build') {
             steps {
                 sh 'echo "Helo Worls"'
                 sh '''
                     echp "Multiline shell steps works too"
                      ls -lah
                  '''
               }
         }
      }
}
